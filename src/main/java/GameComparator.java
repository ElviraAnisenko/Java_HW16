import java.util.Comparator;

public class GameComparator implements Comparator <Player> {
    @Override
    public int compare(Player o1, Player o2) {
        if() {};
        else {}:
        else {
        return 0};
    }
}
